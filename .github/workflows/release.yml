name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore RynorArch.slnx

      - name: Build
        run: dotnet build RynorArch.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test RynorArch.slnx --configuration Release --no-build

      - name: Publish packages
        if: ${{ secrets.NUGET_API_KEY != '' }}
        run: ./publish.ps1 -Increment None -Push -ApiKey "${{ secrets.NUGET_API_KEY }}"
        shell: pwsh
